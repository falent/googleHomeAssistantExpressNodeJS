{
  "name": "simple-skill-example-for-google-home",
  "description": "Examples of DialogFlow  intents.",
  "version": "0.0.1",
  "private": true,
  "license": "Apache Version 2.0",
  "author": "Tomasz Krajewski",
  "engines": {
    "node": "~6.11.1"
  },
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "heroku-deploy": "git checkout atomic-development && git add ./ && git commit -m \"development increment\" && git push heroku atomic-development:master --force && heroku logs --tail",
    "serve": "node index.js"
  },
  "dependencies": {
    "actions-on-google": "^2.0.0",
    "dotenv": "^6.1.0",
    "express": "^4.16.3",
    "request": "^2.88.0",
    "request-promise": "^4.2.2"
  },
  "devDependencies": {
    "eslint": "^4.19.0",
    "eslint-config-google": "^0.9.1"
  }
}
